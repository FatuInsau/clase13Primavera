// sumar(a, b)
// Crear una función sumar que tome como argumentos dos números y devuelva la suma de ellos

function sumar (numero1,numero2) {
    var resultado = numero1 + numero2;
    return resultado;
};

function promedio (num2) {
    var resultadoParcial = sumar(num1,num2);
    var resultadoFinal = resultadoParcial/2;
    return resultadoFinal;
}

console.log( promedio(4,2) )